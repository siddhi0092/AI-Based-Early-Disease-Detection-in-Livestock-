# AI-Based-Early-Disease-Detection-in-Livestock-
Built an aggressive ML model for early mastitis detection using time-series data. Combines statistical and temporal features with a stacking ensemble (XGBoost, Random Forest, Logistic Regression). Uses SMOTE, class weighting, and threshold tuning to achieve high sensitivity and better detection of positive cases.
